#define the countious intergration process

node{
    stage("prepare"){
        echo "prepare env before build"
        sh("ls -a")
    }

    stage("build"){
        echo "build project"
    }

    stage("test"){
        echo "test project"
    }

}